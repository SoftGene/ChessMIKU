{
  "name": "chess-review-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Browser extension of Chess Review: reviews finished chess.com games.",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "vite build --mode background && vite build --mode content"
  },
  "devDependencies": {
    "@types/chrome": "^0.3.0",
    "@types/node": "^24.13.6",
    "jsdom": "^29.1.1",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  },
  "dependencies": {
    "chess.js": "^1.4.0"
  }
}
